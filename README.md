# Maskify: Face Mask Detection Model
A face mask detection model using Convolutional Neural Networks (CNNs) to classify images as either masked or unmasked, trained on Kaggle's face mask detection dataset.
